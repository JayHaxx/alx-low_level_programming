#include <stdlib.h>
#include <stdio.h>

/**
 * mod - divide two integers for remainder
 * @a: first integer
 * @b: second integer
 * Return: Remainder of two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
